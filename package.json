{
  "name": "agent-skills-mcp",
  "version": "1.10.0",
  "description": "AgentSkills MCP server — skills as prompts, resources, and tools (MCP 2025-11-25)",
  "private": true,
  "scripts": {
    "start": "bun src/server.ts",
    "dev": "bun --watch src/server.ts",
    "test": "bun test/integration.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.1"
  }
}
